# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0          # async PostgreSQL driver
alembic>=1.13.1

# Validation
pydantic>=2.7.0
pydantic[email]>=2.7.0   # for EmailStr

# Auth (password hashing — keep auth layer separate from data layer)
passlib[bcrypt]>=1.7.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0            # async test client for FastAPI
